import ApprovalQueue from "./ApprovalQueue";

const ApprovalQueuePage = ({ setLoading, loading }) => (
    <ApprovalQueue setLoading={setLoading} loading={loading} />
);

export default ApprovalQueuePage;
